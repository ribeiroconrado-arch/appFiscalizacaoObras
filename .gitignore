*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Material de planejamento/design — fica só local, não vai pro repositório
/planejamento

# Passo a passo de deploy: caminho e topologia do servidor de produção,
# fica só local — o repositório é público.
/docs/deploy.md

# Cópias do .env feitas antes de mudanças de ambiente: têm senha dentro.
.env.backup*

# Script de deploy: mesma razão do docs/deploy.md — fica só no servidor.
/deploy.sh

